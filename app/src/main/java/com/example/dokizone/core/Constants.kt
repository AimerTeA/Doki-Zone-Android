package com.example.dokizone.core

object Constants {
    object Filter {
        const val BY_POPULARITY = "bypopularity"
    }

    enum class HomeSection {
        RANDOM,
        POPULAR,
        CURRENT_SEASON
    }
}